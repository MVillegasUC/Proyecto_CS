name: CI Básico

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Clonar el repositorio
        uses: actions/checkout@v3

      - name: Verificar que el repositorio se clonó
        run: echo "Todo bien con el flujo de trabajo básico ✅"
